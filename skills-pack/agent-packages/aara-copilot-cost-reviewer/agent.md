# aara-copilot-cost-reviewer (runnable)

The runnable artifact is the wired AaraMinds subagent **`skills-pack/.claude/agents/aara-copilot-cost-reviewer.md`**
(least-privilege: Read/Write/Edit/Glob/Grep, no Bash; `permissionMode: ask`). It routes to the
`copilot-cost-optimization` skill. This file is the package pointer; see AGENT_SPEC.md for the contract.
