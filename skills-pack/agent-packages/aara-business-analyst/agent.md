# aara-business-analyst (runnable)

The runnable artifact is the wired AaraMinds subagent **`skills-pack/.claude/agents/aara-business-analyst.md`**
(least-privilege: Read/Write/Edit/Glob/Grep, no Bash; `permissionMode: ask`). This file is the package's
pointer to the live runnable definition; see AGENT_SPEC.md for the full contract.
