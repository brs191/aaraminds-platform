# Worked example — Leadership Status Agent package

A full `agent-engineering` Create/Review output, applied to the existing `aara-status-deck` capability.
**Illustrative** — behavior is not run-tested, so the release gate is CONDITIONAL PASS (pilot) / FAIL
(production candidate). This is the template for the 3-agent dogfood (status / migration-QA / topology).
See `package-index.md` to start.
